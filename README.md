# Setup the project

### _In the APIWebServices directory :_

## Launch the API service by running :
```bash
dotnet run
```

The API routes dashboard will be accessible at http://localhost:5075

### _In the APIConsumerApp directory :_

## Launch the API consumer app by running :
```bash
dotnet run
```

You will see the results of the fetch called in Program.cs.